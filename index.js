<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Vault Combination</title>
</head>
<body>
    <script>
        // Step 1: Create a greeting message
        var greetingMessage = "You have received this message because you have been chosen to open an important vault. Here is the secret combination:";

        // Step 2: Define variables using different arithmetic operators
        // To get 10:
        var firstCode = 5 + 5; // Addition

        // To get 40:
        var secondCode = 80 / 2; // Division

        // To get 39:
        var thirdCode = 60 - 21; // Subtraction

        // Step 3: Combine results into the final message
        var finalMessage = greetingMessage + "\n" +
                           "Combination: " + firstCode + " - " + secondCode + " - " + thirdCode;

        // Step 4: Display the message
        alert(finalMessage); // This will show the message in a popup dialog
    </script>
</body>
</html>
